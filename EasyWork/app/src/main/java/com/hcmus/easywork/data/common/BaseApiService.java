package com.hcmus.easywork.data.common;

/**
 * The <code>BaseApiService</code> interface is always empty.
 * Only <code>AuthenticatedApiService</code> interface and
 * <code>UnauthenticatedApiService</code> must <code>extends</code> this for unifying repository usage.
 */
public interface BaseApiService {
    // Empty declaration
}
