package com.hcmus.easywork.data.common;

/**
 * The <code>AuthenticatedApiService</code> class is empty, but every API service interface
 * that requires access token must 'extends' this.
 */
public interface AuthenticatedApiService extends BaseApiService {
    // Empty declaration
}
