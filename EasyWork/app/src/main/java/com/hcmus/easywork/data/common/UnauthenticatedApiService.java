package com.hcmus.easywork.data.common;

/**
 * The <code>UnauthenticatedApiService</code> class is empty, but every API service interface
 * that does not require access token must <code>extends</code> this.
 */
public interface UnauthenticatedApiService extends BaseApiService {
    // Empty declaration
}
